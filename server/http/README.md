# HTTP Utilities

Shared helpers for the HTTP layer, such as request context, authentication helpers, and serialization.

## Role in the Architecture

- Supports the routes layer without embedding business logic.
- Provides common behavior for request handling and auditing.
