# Feature Flags Application Services

Manages feature flags, personas, and permissions.

## Role in the Architecture

- Called by `server/routes/feature-flags.ts`.
- Uses storage and audit logging for changes.

