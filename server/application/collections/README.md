# Collections Application Services

Manages user collections of content (create, update, add, remove).

## Role in the Architecture

- Called by `server/routes/collections.ts`.
- Handles storage updates and returns collection state.

