{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Live documentation lookup for libraries used in this project (Drizzle, Stripe, Contentful, React Query, Vitest, Radix UI, etc.)"
    },
    "github": {
      "command": "github-mcp-server",
      "args": ["stdio"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "description": "Native GitHub operations: issues, PRs, workflows, and branch management. Requires github-mcp-server.exe in PATH and GITHUB_PERSONAL_ACCESS_TOKEN user env var set."
    }
  }
}
