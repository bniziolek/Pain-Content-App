# Favorites Application Services

Adds, removes, and lists user favorites.

## Role in the Architecture

- Called by `server/routes/favorites.ts`.
- Stores favorites in the database and returns lists.

