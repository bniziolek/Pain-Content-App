# React Contexts

Global UI state shared across the application.

## Role in the Architecture

- Provides app-wide state (e.g., user, theme, feature flags).
- Keeps state logic out of individual components.

